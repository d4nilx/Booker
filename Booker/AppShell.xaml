<?xml version="1.0" encoding="UTF-8" ?>
<Shell xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
       xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
       xmlns:views="clr-namespace:Booker.Views"
       x:Class="Booker.AppShell"
       Shell.NavBarHasShadow="False"
       BackgroundColor="#F5F0E8">

    <TabBar>
        <!-- Route names must match the strings used in Shell.Current.GoToAsync() calls in ViewModels -->
        <ShellContent Title="Home"
                      Icon="tab_home.png"
                      Route="MainPage"
                      ContentTemplate="{DataTemplate views:MainPage}" />

        <ShellContent Title="Search"
                      Icon="tab_search.png"
                      Route="SearchPage"
                      ContentTemplate="{DataTemplate views:SearchPage}" />

        <ShellContent Title="Library"
                      Icon="tab_library.png"
                      Route="LibraryPage"
                      ContentTemplate="{DataTemplate views:LibraryPage}" />

        <ShellContent Title="Stats"
                      Icon="tab_stats.png"
                      Route="StatsPage"
                      ContentTemplate="{DataTemplate views:StatsPage}" />
    </TabBar>
</Shell>
