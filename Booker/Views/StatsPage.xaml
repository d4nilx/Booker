<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:vm="clr-namespace:Booker.ViewModels"
             x:Class="Booker.Views.StatsPage"
             Title="Stats"
             BackgroundColor="#1C2B1E" Shell.NavBarIsVisible="False">

    <!-- BindingContext is set via the code-behind constructor (DI-injected StatsViewModel) -->

    <ScrollView>
        <VerticalStackLayout Padding="24" Spacing="20">

            <Label Text="Reading Stats"
                   FontSize="28"
                   FontAttributes="Bold"
                   TextColor="#F0EDE4" />

            <!-- Stats grid -->
            <Grid ColumnDefinitions="*,*" RowDefinitions="Auto,Auto"
                  ColumnSpacing="12" RowSpacing="12">

                <Border Grid.Column="0" Grid.Row="0"
                        BackgroundColor="#243327" Stroke="#2E4030">
                    <Border.StrokeShape><RoundRectangle CornerRadius="12"/></Border.StrokeShape>
                    <VerticalStackLayout Padding="16" Spacing="4">
                        <Label Text="{Binding TotalBooks}"
                               FontSize="32" FontAttributes="Bold" TextColor="#F0EDE4"/>
                        <Label Text="Total Books" FontSize="11" TextColor="#7A9E7E" CharacterSpacing="1"/>
                    </VerticalStackLayout>
                </Border>

                <Border Grid.Column="1" Grid.Row="0"
                        BackgroundColor="#243327" Stroke="#2E4030">
                    <Border.StrokeShape><RoundRectangle CornerRadius="12"/></Border.StrokeShape>
                    <VerticalStackLayout Padding="16" Spacing="4">
                        <Label Text="{Binding ReadBooks}"
                               FontSize="32" FontAttributes="Bold" TextColor="#8FBC8F"/>
                        <Label Text="Finished" FontSize="11" TextColor="#7A9E7E" CharacterSpacing="1"/>
                    </VerticalStackLayout>
                </Border>

                <Border Grid.Column="0" Grid.Row="1"
                        BackgroundColor="#243327" Stroke="#2E4030">
                    <Border.StrokeShape><RoundRectangle CornerRadius="12"/></Border.StrokeShape>
                    <VerticalStackLayout Padding="16" Spacing="4">
                        <Label Text="{Binding TotalPagesRead}"
                               FontSize="32" FontAttributes="Bold" TextColor="#F0EDE4"/>
                        <Label Text="Pages Read" FontSize="11" TextColor="#7A9E7E" CharacterSpacing="1"/>
                    </VerticalStackLayout>
                </Border>

                <Border Grid.Column="1" Grid.Row="1"
                        BackgroundColor="#8FBC8F" Stroke="Transparent">
                    <Border.StrokeShape><RoundRectangle CornerRadius="12"/></Border.StrokeShape>
                    <VerticalStackLayout Padding="16" Spacing="4">
                        <Label Text="{Binding ReadPercentage, StringFormat='{0:P0}'}"
                               FontSize="32" FontAttributes="Bold" TextColor="#1C2B1E"/>
                        <Label Text="Completion" FontSize="11" TextColor="#2E4030" CharacterSpacing="1"/>
                    </VerticalStackLayout>
                </Border>
            </Grid>

            <!-- Top Genres -->
            <Label Text="TOP GENRES"
                   FontSize="11"
                   CharacterSpacing="3"
                   TextColor="#8FBC8F"
                   Margin="0,8,0,0"/>

            <CollectionView ItemsSource="{Binding TopGenres}">
                <CollectionView.ItemTemplate>
                    <DataTemplate x:DataType="vm:GenreStat">
                        <Border BackgroundColor="#243327" Stroke="#2E4030" Margin="0,4">
                            <Border.StrokeShape><RoundRectangle CornerRadius="10"/></Border.StrokeShape>
                            <Grid ColumnDefinitions="*,Auto" Padding="16,12">
                                <Label Grid.Column="0"
                                       Text="{Binding GenreName}"
                                       FontSize="14" TextColor="#F0EDE4"/>
                                <Label Grid.Column="1"
                                       Text="{Binding Count}"
                                       FontSize="14" FontAttributes="Bold"
                                       TextColor="#8FBC8F"/>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </CollectionView.ItemTemplate>
            </CollectionView>

        </VerticalStackLayout>
    </ScrollView>
</ContentPage>