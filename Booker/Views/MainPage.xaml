<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:vm="clr-namespace:Booker.ViewModels"
             x:Class="Booker.Views.MainPage"
             Title="Booker"
             BackgroundColor="#1C2B1E" Shell.NavBarIsVisible="False">

    <!-- BindingContext set via code-behind constructor -->

    <ScrollView>
        <VerticalStackLayout Padding="24" Spacing="24">

            <!-- Header -->
            <Label Text="Booker"
                   FontSize="36"
                   FontAttributes="Bold"
                   TextColor="#F0EDE4" />
            <Label Text="Your reading companion"
                   FontSize="13"
                   TextColor="#7A9E7E" />

            <!-- Currently Reading Card -->
            <Frame IsVisible="{Binding HasCurrentBook}"
                   BackgroundColor="#243327"
                   BorderColor="#2E4030"
                   CornerRadius="12"
                   Padding="20"
                   HasShadow="False">
                <VerticalStackLayout Spacing="12">
                    <Label Text="CURRENTLY READING"
                           FontSize="10"
                           CharacterSpacing="3"
                           TextColor="#8FBC8F" />

                    <Grid ColumnDefinitions="80,*" ColumnSpacing="16">
                        <Image Grid.Column="0"
                               Source="{Binding CurrentBook.Thumbnail}"
                               HeightRequest="110"
                               WidthRequest="80"
                               Aspect="AspectFill">
                            <Image.Clip>
                                <RoundRectangleGeometry CornerRadius="6" Rect="0,0,80,110" />
                            </Image.Clip>
                        </Image>

                        <VerticalStackLayout Grid.Column="1" Spacing="6" VerticalOptions="Center">
                            <Label Text="{Binding CurrentBook.Title}"
                                   FontSize="16"
                                   FontAttributes="Bold"
                                   TextColor="#F0EDE4"
                                   MaxLines="2"
                                   LineBreakMode="TailTruncation" />
                            <Label Text="{Binding CurrentBook.Author}"
                                   FontSize="12"
                                   TextColor="#7A9E7E" />
                            <Label TextColor="#7A9E7E" FontSize="11">
                                <Label.FormattedText>
                                    <FormattedString>
                                        <Span Text="{Binding CurrentBook.PagesRead}" />
                                        <Span Text=" / " />
                                        <Span Text="{Binding CurrentBook.PageCount}" />
                                        <Span Text=" pages" />
                                    </FormattedString>
                                </Label.FormattedText>
                            </Label>

                            <!-- ReadingProgress is now a proper computed property on the ViewModel -->
                            <ProgressBar Progress="{Binding ReadingProgress}"
                                         ProgressColor="#8FBC8F"
                                         BackgroundColor="#2E4030"
                                         HeightRequest="4" />
                        </VerticalStackLayout>
                    </Grid>
                </VerticalStackLayout>
            </Frame>

            <!-- Empty state -->
            <Frame IsVisible="{Binding HasCurrentBook, Converter={StaticResource InvertedBool}}"
                   BackgroundColor="#243327"
                   BorderColor="#2E4030"
                   CornerRadius="12"
                   Padding="32"
                   HasShadow="False">
                <VerticalStackLayout Spacing="8" HorizontalOptions="Center">
                    <Label Text="📚" FontSize="40" HorizontalOptions="Center" />
                    <Label Text="No book in progress"
                           FontSize="16"
                           FontAttributes="Bold"
                           TextColor="#F0EDE4"
                           HorizontalOptions="Center" />
                    <Label Text="Search for a book and add it to your library"
                           FontSize="12"
                           TextColor="#7A9E7E"
                           HorizontalOptions="Center"
                           HorizontalTextAlignment="Center" />
                </VerticalStackLayout>
            </Frame>

            <!-- Quick nav buttons — GoToLibraryCommand and GoToSearchCommand now exist -->
            <Grid ColumnDefinitions="*,*" ColumnSpacing="12">
                <Button Grid.Column="0"
                        Text="My Library"
                        BackgroundColor="#8FBC8F"
                        TextColor="#1C2B1E"
                        CornerRadius="10"
                        HeightRequest="50"
                        Command="{Binding GoToLibraryCommand}" />

                <Button Grid.Column="1"
                        Text="Search Books"
                        BackgroundColor="Transparent"
                        TextColor="#F0EDE4"
                        BorderColor="#2E4030"
                        BorderWidth="1"
                        CornerRadius="10"
                        HeightRequest="50"
                        Command="{Binding GoToSearchCommand}" />
            </Grid>

        </VerticalStackLayout>
    </ScrollView>
</ContentPage>
